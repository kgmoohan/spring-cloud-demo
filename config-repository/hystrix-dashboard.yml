spring:
  application:
    name: hystrix-dashboard

server:
  port: 7213

eureka:
  instance:
      hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/