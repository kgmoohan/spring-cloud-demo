spring:
  application:
    name: feign-service

server:
  port: 7211

eureka:
  instance:
      hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/